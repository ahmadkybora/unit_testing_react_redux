// حتما باید یک مقدار برگرداند
// بدون مقدار خطا
// میدهد
// undefiend
// هم خطا میدهد 
const carts=[];
export default (state=null, action) => {
    // در صورتی که مقدار false برگرداند
    // switch(action.type) {
    //     default: return state;
    // }
    return state;
}